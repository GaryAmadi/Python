#Preceeded by an identifier
def hello(first_name, last_name):
  print("Hello " + first_name + " " + last_name)
  
hello(last_name="Amadi", first_name="Gary")

#before passing in the arguments for parameters when calling a function, the arguments are assigned to a specific parameter.
#Here order of assignment doesn't matter as long as the desired argument is assigned to the correct parameter.
